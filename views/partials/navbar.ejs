<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Kicks On Fly</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <% if (user) { %>
            <a class="nav-link" href="/" <%- title === 'All Collection' ? 'class="active"' : '' %>>Home</a>
            <a class="nav-link" href="/sneakers/new" <%- title === 'Add Sneaker' ? 'class="active"' : '' %>>Add Sneaker</a>
            <a class="nav-link" href="/apparels/new" <%- title === 'Add Apparel' ? 'class="active"' : '' %>>Add Apparel</a>
            <a class="nav-link" href="/logout">Log Out</a>
            <% } else { %>
              <a class="nav-link" href="/auth/google">Log In</a>
              <% } %>
            </div>
          </div>
        </div>
      </nav>
      